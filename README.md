# 🎬 WanVideo Pro 2.2 — Text-to-Video Generation Engine

> **Неофициальная реализация архитектуры Wan 2.2** для генерации видео по текстовым промптам (Text-to-Video).
> Запускается локально без Docker и прав суперпользователя — только Python и виртуальное окружение.

---

## 🧠 Архитектура

| Компонент | Описание |
|---|---|
| **Massive Diffusion Transformer (DiT)** | Основное ядро с адаптивной нормализацией `adaLN-Single`; обрабатывает видеопатчи |
| **Causal 3D VAE** | Сжимает пространство кадров в латентное представление с учётом причинно-следственных связей |
| **Flow Matching** | Прямые траектории сэмплирования — быстрее стандартной диффузии |
| **T5-v1.1 Text Encoder** | Кодировщик текстовых промптов с глубоким пониманием контекста |

---

## ✨ Возможности

- 🐍 **Pure Python Backend** — никаких Docker-контейнеров и root-прав, запускается в любом `venv`
- �️ **ComfyUI Custom Node** — встраивается в ComfyUI-пайплайны как нода `Wan Video Generator (T2V)`
- ☁️ **Interactive Web UI (Veo 3.1 API)** — React-фронтенд для облачной генерации через Veo 3.1 Fast & HQ (для систем без мощной GPU)

---

## ⚙️ Установка

```bash
git clone https://github.com/FBNonaMe/Wan2.2-Uncensored-Clone.git
cd WanVideo-Clone-T2V

python -m venv venv

# Linux / macOS
source venv/bin/activate

# Windows
venv\Scripts\activate

pip install -r requirements.txt
```

---

## � Использование

### Python Script

```python
from pipeline.video_pipeline import WanFlowMatchingPipeline

pipe = WanFlowMatchingPipeline(device="cuda")

video = pipe(
    "A beautiful sunset over the ocean, cinematic, 4k resolution",
    num_frames=16
)
```

### ComfyUI

1. Скопируйте папку проекта в директорию `custom_nodes` вашего ComfyUI.
2. Перезапустите ComfyUI.
3. Найдите ноду **`Wan Video Generator (T2V)`** в меню нод.

---

## 📋 Требования

- Python 3.10+
- PyTorch 2.x с поддержкой CUDA (рекомендуется GPU с 8+ GB VRAM)
- Windows / Linux

---

## ⚠️ Отказ от ответственности

Этот проект предназначен **исключительно для исследовательских и образовательных целей**.
Пользователь несёт полную ответственность за соблюдение применимого законодательства
и этических норм при использовании данного программного обеспечения.
Авторы **не несут ответственности** за любое неправомерное применение.
